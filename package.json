{
  "name": "cypressautomation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open_CinchTests": "npx cypress open --env ENVIRONMENT=CinchTests",
    "cypress:run_CinchTests": "npx cypress run --env ENVIRONMENT=CinchTests",
    "cypress:open_ExamAppTests": "npx cypress open --env ENVIRONMENT=ExamAppTests",
    "cypress:run_ExamAppTests": "npx cypress run --env ENVIRONMENT=ExamAppTests",
    "cypress:open": "cypress open"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.3.1",
    "cypress-iframe": "^1.0.1"
  }
}
